============
Installation
============

At the command line::

    $ easy_install django-simple-cms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-simple-cms
    $ pip install django-simple-cms
